{
  "name": "patch-sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Patch SDK monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:react": "npm run build --workspace=@patch-sdk/react",
    "publish:react": "npm publish --workspace=@patch-sdk/react --access public"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
